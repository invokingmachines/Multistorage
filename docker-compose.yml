version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: multistorage-postgres
    environment:
      POSTGRES_DB: multistorage
      POSTGRES_USER: multistorage
      POSTGRES_PASSWORD: multistorage
    ports:
      - "5432:5432"
